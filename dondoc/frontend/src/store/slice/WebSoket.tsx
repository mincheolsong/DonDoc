import { createSlice } from '@reduxjs/toolkit';

  
const initialState = {

  }

const WebSoketSlice = createSlice({
    name: 'userSlice',
    initialState,
    reducers: {
    }
   
  });
  
  
  export const {} = WebSoketSlice.actions;
  
  export default WebSoketSlice; 
  